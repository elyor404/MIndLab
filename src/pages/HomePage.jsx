import EmptyState from '../components/EmptyState/EmptyState'

const HomePage = () => {
  return (
    <EmptyState
      title="No items yet"
      message="Add content to see progress, deadlines, analytics and activity."
    />
  )
}

export default HomePage

